{% block body %}
    <header class="bg-gradient-to-br from-blue-600 to-blue-950 text-white p-4 mb-4">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-between">
                <h1 class="text-2xl cursor-default hover:rotate-360 transition duration-1000 hover:text-blue-200">CarSeller</h1>
                <nav>
                    <div class="flex gap-2">
                        <a href="{{ path('app_home') }}" class="header-button">Accueil</a>
                        <a href="{{ path('app_vehicle_rental') }}" class="header-button">Locations</a>
                        <a href="{{ path('app_vehicle_sale') }}" class="header-button">Ventes</a>
                        {% if app.user %}
                            <a href="{{ path('app_customer') }}" class="header-button">Mon compte</a>
                            <a href="{{ path('app_logout') }}" class="header-button">Déconnexion</a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="header-button">Connexion</a>
                            <a href="{{ path('app_register') }}" class="header-button">Inscription</a>
                        {% endif %}
                    </div>
                </nav>
            </div>
        </div>
    </header>
{% endblock %}
