{% block body %}
    <header class="bg-gradient-to-r from-blue-600 to-black text-white p-4 mb-4">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-between">
                <h1 class="text-2xl">CarSeller</h1>
                <nav>
                    <div class="flex gap-2">
                        <a href="{{ path('app_home') }}" class="text-white hover:text-blue-200 px-2 cursor-pointer">Accueil</a>
                        <a href="{{ path('app_vehicle_locations') }}" class="text-white hover:text-blue-200 px-2 cursor-pointer">Locations</a>
                    </div>
                </nav>
            </div>
        </div>
    </header>
{% endblock %}
